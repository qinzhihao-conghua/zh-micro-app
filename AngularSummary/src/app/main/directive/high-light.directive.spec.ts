import { HighLightDirective } from './high-light.directive';

describe('HighHightDirective', () => {
  it('should create an instance', () => {
    const directive = new HighLightDirective();
    expect(directive).toBeTruthy();
  });
});
