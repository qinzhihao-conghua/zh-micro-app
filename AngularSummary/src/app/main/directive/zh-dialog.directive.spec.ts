import { ZhDialogDirective } from './zh-dialog.directive';

describe('ZhDialogDirective', () => {
  it('should create an instance', () => {
    const directive = new ZhDialogDirective();
    expect(directive).toBeTruthy();
  });
});
